"use client";

import PlaceholderTemplate from "../placeholder/placeholder-template";
import type { TemplateProps } from "../types";

export default function ArchiveOsTemplate(props: TemplateProps) {
  return <PlaceholderTemplate {...props} />;
}
