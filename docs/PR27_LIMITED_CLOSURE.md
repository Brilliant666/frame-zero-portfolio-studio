# PR27 有限修复收口

## 范围与交付状态

本轮仅修复 F1–F4 和全量门禁暴露的两个直接阻断，不重新设计、不写真实内容、不部署、不合并。起始提交 `60946e7`；代码验证提交 `3eb0f55`，后续仅追加本记录。最终 head、四项 CI 的 run ID/attempt/实际测试 SHA 以 PR #27 描述为准；本记录不以旧 head 的绿色结果代替最终 CI。

## 四项结论

- **F1 已复现并修复**：旧测试将互斥分支的两个源码引用误判为实际重复。现在执行真实模板组件的服务端渲染，分别验证旧版与新版联系区域，并覆盖十一模板；每次只有一个共享 PlatformAccounts 实例及账号区域，包括隐藏 DOM 检查。没有改变页面设计、比例、链接或安全降级。
- **F2 已复现并修复**：旧测试仍要求 15 项清单，实际审核清单已是 117 项。旧 Docker 的目录否定规则允许子孙文件继承放行，因此同一旧 head 的真实 Container 成功与静态契约失败可以并存，不代表字体必然丢失。现在每层目录重新默认拒绝后逐文件开放，并补齐构建辅助模块 COPY；测试使用独立明确的预期集合。CI 校验真实 builder 公共文件树和私有哨兵，执行无缓存容器构建、运行时资源验证。
- **F3 已复现并修复**：替换 ±50 处有断点的双系数公式为连续指数曲线（0.0016）；极端指数有限钳制，无效值不移动相机。普通/Ctrl 缩放、Shift 平移、指针锚点、单位归一化及边界不变。真实函数测试覆盖正负 49/50/51、分数、连续事件、逆向、极端值；浏览器 CDP 事件回放未发生整页滚动，不冒充实体鼠标验收。
- **F4 已复现并修复**：真实组件在 DPR 2、显示需求超过 600px 时，旧实现等待 5.5 秒仍停在 600；修复后无需操作，约 1.8 秒真实请求并解码为 1100。有限调度在保护期结束补查，结合可见性/尺寸/镜头/手机滚动/恢复事件；同档去重、先解码后替换、有界重试、卸载忽略旧任务、低档不覆盖高档。初始仍为 600，并保留 1100/2200 层级及源尺寸上限。实际显示失败也可回退并有限重试。

F4 浏览器证据包含：保护期内缩放后自动升级、模式切换保留高清、减少动态效果、慢图保持低档、503 后一次重试、足够清晰时不请求高档、390px 手机自然滚动后的新照片升级。定时器及控制器测试另覆盖取消、隐藏恢复与乱序。

## 字体与产物

普通生产和本机正式预览均保留同一已审核公共清单：15 项既有资源、101 个思源宋体 Heavy WOFF2 子集和 OFL，共 117 项；字体总计 3,161,328 字节，来源摘要与许可证不变。来源说明及 source manifest 不作为运行时公共文件开放。未放开整个 public，也未引入私人照片、catalog、数据库或环境文件。

隔离目录从 git archive 与必要修改构建、重新 npm ci，两套产物均通过；101 字体逐个 HTTP 200、类型/魔数/字节/SHA 正确。浏览器在正式预览中实际使用自托管 Noto Serif SC，而非 SimSun 回退。普通生产 `/preview` 仍为 404，本机正式预览可用；没有恢复公网新版入口。真实 Linux 无缓存容器结果见最终 PR CI。

## 完整本地验证

- 最终 `npm test` 完整运行所有阶段：498 项测试通过，包括两套构建与预算、运行时和打包契约；不是只重跑第一个失败套件。
- lint、TypeScript、`git diff --check`、公共安全检查通过；本机正式预览构建通过。
- Next：app JS 306.0 KiB、bootstrap 640.5 KiB、CSS 300.0 KiB；legacy：public JS 550.0 KiB、全部路由 JS 659.1 KiB、CSS 270.5 KiB。预算未放宽。
- 本地使用已有捆绑 Node 24.19.0；CI 使用仓库既定 Node 22.13.1，未升级依赖或仓库 Node 配置。
- 两个额外直接阻断：lint 排除生成的 `.next-local-preview`；后台环境测试精确允许既有强制关闭新版的值 `0`，继续拒绝其他公开环境注入。没有修改实际安全边界。

## 有限性能复测

代码 `3eb0f55`，本机正式构建，Chrome 154.0.8037.57，1440×900、DPR 1，同组 11 张照片；每次独立浏览器上下文，悬停组预热 600ms。本机图片服务缓存复用/未知，因此“未悬停”不是冷服务缓存。录屏与 trace 同时开启，headless rAF 中位约 3.1ms，非强制 60Hz。口径为 DroppedFrame / (DrawFrame + DroppedFrame)。

| 场景 | 0–1.5 秒三次 | 中位数 | 1.5–5 秒三次 | 中位数 |
| --- | --- | --- | --- | --- |
| 未悬停 | 6.73 / 6.54 / 5.74% | 6.54% | 5.99 / 3.07 / 6.45% | 5.99% |
| 悬停预热 | 6.38 / 6.56 / 6.57% | 6.56% | 6.50 / 6.50 / 3.12% | 6.50% |

默认尺寸不一定触发高清请求，上表后窗不能直接称为高清升级成本。另行核对 DPR 2 和保护期内缩放的真实升级后窗，原始样本与最终汇总附在 PR 描述/本机证据。旧记录 7.01/7.79% 与当前环境不能严格视为同条件提升。

`PERFORMANCE_TARGET = NOT_MET`：仍高于 5%，不自动豁免，不扩大动画优化范围，等待人工决定。

## 回归、数据和证据

浏览器检查 1440×900、1024×768、390×844、320px；覆盖旧首页/后台、十一模板、新版三构图/双主题、图集进入退出、大图前后/Esc/焦点恢复，以及进场时快速切套餐/联系无飞行残影。自动化设备事件回放不能替代实体鼠标、触控板和真机触控体验验收。

新旧 GET 前后逻辑内容 SHA 一致（包含封面、成员和顺序）；旧记录 1 的 updatedAt 仍为 `2026-09-24 15:16:49`，新版 2601 的 revision 仍为 7、updatedAt 仍为 `2026-09-24T09:58:05.181Z`。没有真实内容 PUT。保存链/schema/CAS 未修改；本机正式 runner 无 D1，不将其视为生产真实保存链验收。

截图、录屏、trace 和私有数据摘要仅留本机 `pr27-limited-closure/` 证据目录，不纳入 Git。关键文件：`npm-test.log`、`f4-baseline.json`、`f4-final.json`、`f4-mobile.json`、`smoke.json`、`fonts-production.json`、`perf-stats.json`、`data-before.json`、`data-after.json`。

## PR 承接与人工边界

PR #26 head `2a39ba0` 仍被 #27 完整包含；代码验证提交相对它领先 28、落后 0，收口文档再增加一次提交。#27 是包含 #26 的累计整合审查；获准合并 #27 后，再由人工决定关闭被替代的 #26。未合并、关闭、改 base、删分支、开启 auto-merge 或部署。

`HUMAN_VISUAL_APPROVAL = PENDING`

`HUMAN_MERGE_APPROVAL = REQUIRED`

`ENGINEERING_LAUNCH_LINE = FROZEN_AT_REPO_SIDE_BOOTSTRAP_READY`
