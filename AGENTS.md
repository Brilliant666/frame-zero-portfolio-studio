# Project execution entry

Read [docs/CURRENT_STATUS.md](docs/CURRENT_STATUS.md) first for the single current
checkpoint and `PRODUCT_DELIVERY_MISSION_02` authority. Then read its linked
North Star, roadmap and relevant Accepted ADRs. Resume the recorded slice;
do not repeat historical audits or Docker recovery without new evidence.

Protect existing edits and private `public/photos` (never stage it). Stage
specific paths only. Merge and destructive/remote operations require authority.
Local product development and remote deployment are separate. Preserve real
Site authorization, resource ownership and Published-only public rendering.
